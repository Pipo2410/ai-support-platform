{
  "rewrites": [{ "source": "/", "destination": "/demo.html" }],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "buildCommand": "npm run build"
      }
    }
  ]
}
